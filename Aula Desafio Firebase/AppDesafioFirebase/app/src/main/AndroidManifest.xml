<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appdesafiofirebase">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".view.salvarjogoview.SalvarJogoActivity"></activity>
        <activity android:name=".view.adicionarjogoview.AdicionarJogoActivity" />
        <activity android:name=".view.detalhesjogoview.DetalhesJogoActivity" />
        <activity android:name=".view.SplashScreenActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.createaccountview.CreateAccountActivity" />
        <activity android:name=".view.loginview.LoginActivity" />
        <activity android:name=".view.listajogosview.MainActivity" />
    </application>

</manifest>